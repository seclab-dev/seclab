# Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，并遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 不兼容变更

- Docker 概览、容器历史资源和磁盘统计接口已调整，旧版字段不再兼容。

### Added

- **Docker 概览**
  - 新增项目健康状态、容器状态分布、资源采样覆盖率和数据新鲜度展示。
  - 新增镜像、容器、数据卷与构建缓存的磁盘占用及可回收空间展示。
  - 资源趋势支持 1、6、12 小时时间范围，并可同时对比最多 5 个容器。

### Changed

- Docker 客户端支持 API 版本协商和旧版磁盘统计，最低支持 Docker Engine 28.0。
- Docker 概览 CPU 明确区分宿主机容量占比与容器多核占用，内存采用工作集口径。
- Docker 资源采样调整为每分钟一次，网络趋势直接展示采样速率。
- 精简 Docker 系统信息，并优化系统清理过程中的通知与页面交互。

### Fixed

- 套件工作负载创建前检查目标节点宿主机端口，端口冲突时不再创建容器。
- 套件工作负载容器启动失败时自动清理已创建容器，避免遗留停止状态的容器。
- 修复 Compose 项目数量按容器数量重复统计的问题。
- 修复多核 CPU 占用被错误限制为 100% 以及部分采样被静默忽略的问题。
- 修复统计失败被显示为零负载或 Docker 服务停止的问题。
- 修复容器重启、计数器回退和采样间断导致网络趋势失真的问题。
- 修复 Docker 镜像共享层和构建缓存可回收空间统计与 `docker system df` 不一致的问题。
- 修复网络速率低于 `1 B/s` 时单位显示异常的问题。

## [0.1.0-alpha.2] - 2026-07-11

### 不兼容变更

- 本版本不兼容 `0.1.0-alpha.1` 的协议仿真内置模块。协议仿真已从主控和 Agent 内置功能拆分为 `seclab.protocol-simulation` Compose 套件，旧的主控仿真接口、内置仿真前端、仿真数据库表和 Agent 内置协议运行器不再作为主控能力提供。
- Alpha 阶段不提供旧版本数据迁移兼容保证。升级到本版本后，协议仿真应通过套件中心安装协议仿真套件，并在套件内重新导入规则包和部署实例。
- 套件、桌面快捷方式和安装任务接口的部分请求格式已调整，前端和调用方需要使用当前版本 API。

### Added

- **协议仿真套件化**
  - 支持由协议仿真套件通过 Agent suite workload API 拉起独立规则容器。
  - 支持套件 workload 容器命名、标签归属、停止删除和卸载清理，避免套件卸载后遗留孤儿容器。
  - 支持由 Agent 在宿主机侧执行 PCAP 取证，并将取证结果返回给套件保存。

- **套件中心**
  - 支持按节点安装、启用、停用和卸载套件，套件状态随当前节点上下文展示。
  - 支持安装进度查询和取消安装。
  - 支持安装前解析 Compose 镜像和 `runtime.images` 依赖镜像，并在目标节点自动准备缺失镜像。
  - 支持套件生命周期事件日志。

- **Docker 管理**
  - 新增镜像仓库配置与拉取流程优化。
  - 增强镜像分发进度和可靠性。
  - 在 Docker 项目和容器列表中展示套件资源标记。
  - 支持解析容器日志中的 ANSI 颜色和文本样式，日志面板与日志弹窗可按终端输出效果展示彩色日志。

- **安全加固**
  - 新增安全入口机制，支持随机登录路径、入口记忆和未携带入口时的根路径提示页。
  - 安装流程支持生成初始账号、密码和安全入口，并在服务首次启动时写入安全配置。
  - 新增验证码、防爆破、密码复杂度和基础密码长度校验。
  - 新增安全设置接口和前端安全设置能力。
  - `slctl` 新增登录地址信息查看、密码修改、用户名修改和安全入口管理能力。

### Changed

- 桌面快捷方式保存接口改为在请求体中传递节点 ID。
- Docker 容器详情、日志、历史指标和终端等操作按容器所属节点转发，避免子节点容器请求落到本地节点。
- 套件 Web 入口启用流程会等待入口就绪后再完成启用。
- 套件代理错误会透传上游失败信息，便于定位套件服务异常。
- 优化应用库内置应用排序，优先展示套件中心、节点管理和核心运维工具。
- 优化默认桌面应用组合，默认展示更贴近 Linux 管理场景的核心工具入口。

### Removed

- 移除主控内置协议仿真 API、前端页面、规则包服务和仿真数据库模型。
- 移除 Agent 内置 HTTP、Redis、SMTP、POP3、IMAP、SSH、FTP、RDP 协议仿真运行器。
- 移除主控内置协议仿真应用图标和相关菜单入口。

### Fixed

- 修复桌面快捷方式交换位置时的布局保存问题。
- 修复删除当前节点后没有自动切回本地节点的问题。
- 修复活跃节点仍显示删除入口的问题。
- 修复套件安装后 Web 入口未就绪导致打开失败的问题。
- 修复应用库中内置应用和套件应用卡片对齐方式不一致的问题。
- 修复发布工作流中的 release tag 格式问题。

## [0.1.0-alpha.1] - 2026-06-28

### Added

- **Web 控制台**
  - 提供桌面式多窗口控制台，支持应用打开、拖动、缩放、置顶、最小化和桌面图标管理。
  - 提供应用库、通知历史、平台日志、系统设置和主题切换能力。
  - 提供中英文界面语言切换。

- **节点管理**
  - 支持本地节点和远程节点纳管。
  - 支持节点注册、健康检查、状态监控、基础资源指标展示和节点退役。
  - 支持基于 mTLS 的主控与 Agent 安全通信。

- **Docker 管理**
  - 支持容器、镜像、数据卷、网络和 Compose 项目的可视化管理。
  - 支持容器日志查看、Compose 项目部署和镜像仓库与代理配置。
  - 对套件专属 Docker 资源展示来源标记。

- **Compose 套件中心**
  - 支持导入 `.slsp` 套件包。
  - 支持套件安装、启用、停用、卸载和删除。
  - 支持套件应用入口注册到应用库和桌面。
  - 支持通过主控代理打开套件 Web 应用，并与套件 SDK 协同完成聚焦、通知和语言同步。

- **协议仿真**
  - 支持协议仿真规则库导入和版本校验。
  - 支持仿真实例创建、部署、运行和状态查看。
  - 支持仿真实例 PCAP 取证和结果查看。

- **任务与脚本**
  - 支持脚本管理、脚本编辑和节点脚本执行。
  - 支持一次性计划任务和定时任务下发到 Agent 执行。

- **主机运维**
  - 提供文件管理、文件编辑、进程管理、磁盘管理、防火墙管理和交互式终端。
  - 支持通过节点执行面完成宿主机运维操作。

- **升级与审计**
  - 支持主控与 Agent 在线升级流程。
  - 支持平台事件日志、运行日志和关键操作审计。

[0.1.0-alpha.2]: https://github.com/seclab-dev/seclab/releases/tag/0.1.0-alpha.1...0.1.0-alpha.2
[0.1.0-alpha.1]: https://github.com/seclab-dev/seclab/releases/tag/0.1.0-alpha.1
