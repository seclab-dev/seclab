# Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，并遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 不兼容变更

- “通知历史”已重构为个人“通知中心”；旧通知数据、应用 ID、创建、清空和删除接口不再兼容。
- “平台日志”已拆分为“操作日志”和“运行日志”；旧日志接口、字段和历史记录不再兼容。
- Docker 概览、容器历史资源和磁盘统计接口已调整，旧版字段不再兼容。
- Docker 日志已重构，旧日志接口和历史记录不再兼容。
- Docker 数据卷接口已改为规范化模型，旧版 Bollard 原始字段不再兼容。
- Docker 镜像列表、删除和分发接口已改为规范化模型，旧版镜像字段与逐节点分发接口不再兼容。
- Docker 容器列表、详情、创建、生命周期操作和资源统计接口已调整，旧版原始字段与通用动作接口不再兼容。
- Docker 项目查询、配置和生命周期接口已调整，旧版项目字段与同步操作接口不再兼容。
- 系统监控概览、趋势与设置接口已重建，旧历史、采集器开关、告警阈值和前端类型不再兼容。
- 宿主机终端接口与通信协议已重建，旧终端地址和消息格式不再兼容。
- 文件管理接口已重建，旧版文件列表、读写、上传和下载接口不再兼容。
- 进程管理接口已重建，旧版实时连接地址、消息格式和基于 PID 的信号接口不再兼容。
- 计划任务定义、运行记录和接口已重建，旧版字段与接口不再兼容。
- 脚本库资产、执行记录和接口已重建，旧版字段、同步执行方式和通用命令入口不再兼容。
- 磁盘管理接口与设备模型已重建，旧版按设备名直接分区、格式化和挂载的接口不再兼容。

### Added

- **命令行管理**
  - `slctl stop` 可自动停止当前节点上的全部 SecLab 服务，也可指定只停止 Master 或 Agent 服务。

- **通知中心**
  - 新增面向当前用户的通知中心，集中展示后台任务终态、节点离线与恢复以及账户安全提醒。
  - 支持未读角标、标记已读或未读、全部已读、归档和恢复；通知保留 90 天且不能由客户端伪造或永久删除。
  - 顶部通知入口会在页面可见时静默更新未读数量，刷新失败时保留原有角标和列表内容。
  - 通知列表将提示、警告和严重作为独立关注级别，并单独展示后台任务结果；节点状态与安全提醒不再显示误导性的成功或失败。
  - 支持按读取状态、分类、关注级别、来源模块、时间和关键词筛选，并可按需查看脱敏详情和安全跳转入口。
  - 普通成功、失败和校验提示不再写入历史记录，通知加载失败也不会反复生成新的通知。

- **操作日志与运行日志**
  - 新增全平台统一的操作日志，集中展示 Master 与各节点的可信状态变更、安全事件、最终结果和安全错误摘要。
  - 操作目标显示节点、脚本、任务、文件等领域名称；内部 UUID 仅在详情或任务标识中提供。
  - 操作日志列表新增稳定序号，并在用户或来源名称后展示可信客户端 IP；目标信息集中到详情中查看，时间统一为清晰的数字格式。
  - 操作日志支持时间、结果、模块、影响级别和关键词筛选；后台刷新失败时会保留已有记录，快速筛选时旧响应不会覆盖新结果。
  - Docker 操作日志改为使用同一份全平台记录，节点断网或重启后会继续补报尚未送达的最终结果。
  - 新增独立运行日志应用，可按 Master、Agent、节点和日志文件查询，并区分节点离线、服务停止、尚无文件和筛选无结果。
  - 运行日志将级别、日志源和关键词收纳到可折叠筛选区，常用查询与刷新操作保持紧凑对齐。
  - 运行日志查询支持安全的文件轮转恢复、单次扫描上限和敏感内容脱敏，不再持续轮询大文件。

- **防火墙管理**
  - 支持按引擎、协议族、动作和关键词筛选实际生效的防火墙规则，并通过稳定分页查看大规模规则集。
  - 新增规则详情、采集时间、来源覆盖率和部分数据提示；刷新失败时保留上一次成功读取的规则。
  - 无法读取规则时提供可随语言切换的恢复提示，刷新状态在窄窗口中保持单行显示。
  - 防火墙窗口支持安全跟随当前节点切换，快速切换时旧节点响应不再覆盖当前规则。

- **磁盘管理**
  - 磁盘清单会区分受管、外部、系统和未知设备；设备事实不完整、身份不足或存储拓扑复杂时自动进入只读保护。
  - 创建分区、格式化分区和挂载数据卷现在是三个独立操作；格式化后可保持未挂载，稍后再选择 `/mnt` 下的挂载位置。
  - 支持显式接管已有 ext4 文件系统，以及在不格式化数据的情况下接管当前挂载。
  - 已挂载到 `/mnt` 的普通 ext4 数据卷可保持路径直接接管；Agent 重启后会从 SecLab 持久挂载配置恢复受管状态。
  - 受管数据卷支持安全更改挂载位置；卸载或迁移后会删除旧空目录，非空目录会保留并显示警告。
  - 非系统数据盘即使已有挂载也可执行独立的清盘并重新分区；操作会先安全移除挂载引用，并要求核对设备和事实指纹后输入精确确认文本。
  - 磁盘变更改为可恢复跟踪的后台操作，支持安全阶段取消，并在刷新或重新打开页面后继续查看进度和最终结果。

- **脚本库**
  - 脚本执行改为可恢复跟踪的后台运行，支持查看排队、执行、成功、失败、超时和取消状态。
  - 新增全局执行记录和受限输出摘要；删除脚本后，已有执行历史仍可查询。
  - 脚本保存支持版本冲突检测，外部修改不再静默覆盖当前编辑内容。
  - 脚本执行和最终结果会记录可信用户、来源地址与追踪 ID，且不会记录脚本正文或输出。

- **计划任务**
  - 立即执行改为可恢复跟踪的后台运行，支持查看排队、执行、成功、失败、超时和取消状态。
  - 新增运行取消、受限输出摘要、批量操作结果和独立的执行节点迁移流程。
  - 每个任务可选择 IANA 时区，并按五段、分钟级 Cron 计算执行计划。
  - 任务提交和最终结果会记录可信用户、来源地址与追踪 ID，且不会记录命令或输出内容。

- **系统监控**
  - 新增五秒实时概览、一分钟历史采样以及 1、3、7 天历史保留设置。
  - 新增采集覆盖率、部分数据、过期数据和不可用状态提示，缺失指标不再显示为零值。
  - 监控设置迁移至“设置 > 监控”，支持查看样本数量、最早和最新采样时间并安全清空历史。

- **宿主机终端**
  - 工具栏明确展示目标节点，终端窗口在打开期间固定绑定该节点。
  - 新增 25 分钟空闲提醒和 30 分钟自动断开，断开后保留会话输出与真实退出状态。
  - 终端会话的开始与最终结果会记录到操作日志，但不会记录命令、输入、输出、环境变量或终端转录。

- **文件管理**
  - 目录浏览支持稳定的服务端分页、真实条目统计和文件归属提示。
  - 文件保存支持版本冲突检测与原子提交，外部修改不再被静默覆盖。
  - 文件编辑器支持独立的多标签撤销、光标与滚动状态，并在重新加载前保护未保存内容。
  - 文件保存会保留访问控制和安全属性；为避免破坏链接关系，多硬链接文件不会被直接编辑。
  - 复制、移动和删除改为可恢复跟踪的后台任务，页面恢复后可继续获取最终结果。
  - 上传支持分块续传与完整性校验，下载支持大文件流式传输和断点范围请求。
  - 文件变更、后台任务和传输终态会记录可信用户、来源地址、追踪 ID 与安全结果摘要。

- **进程管理**
  - 进程与网络连接列表支持稳定分页、服务端筛选和采样覆盖率提示，刷新失败时保留上一次成功数据。
  - 进程信号使用稳定进程身份，避免 PID 被复用后误操作其他进程。
  - 强制结束进程需要两次确认和短时一次性凭证，信号提交结果会准确区分已投递与结果未知。

- **Docker 日志**
  - Docker 变更操作现在统一显示在全平台操作日志中，并区分普通操作、高影响操作和错误。
- **Docker 概览**
  - 新增项目健康状态、容器状态分布、资源采样覆盖率和数据新鲜度展示。
  - 新增镜像、容器、数据卷与构建缓存的磁盘占用及可回收空间展示。
  - 资源趋势支持 1、6、12 小时时间范围，并可同时对比最多 5 个容器。
- **Docker 网络**
  - 新增网络归属、驱动和名称筛选，支持按需查看地址配置与容器端点详情。
  - 新增 Bridge 网络的 IPv4、IPv6、内部网络、驱动选项和标签配置。
- **Docker 数据卷**
  - 新增数据卷归属和名称筛选，并支持查看挂载配置及引用容器详情。
  - 新建本地卷支持高级选项和自定义标签配置。
- **Docker 镜像**
  - 镜像仓库支持 Docker Hub 搜索及公共、免认证仓库的完整镜像引用拉取。
  - 镜像分发支持批量任务进度、取消、失败节点重试及页面恢复后继续跟踪。
- **Docker 容器**
  - 新增按名称、镜像、状态和归属筛选容器，并支持最多 50 个容器的批量生命周期操作。
  - 新建容器支持端口、挂载、环境变量、重启策略和网络等结构化配置。
  - 容器详情新增配置、网络、挂载、进程和资源趋势视图，日志支持搜索、暂停与恢复。
- **Docker 项目**
  - 新增项目名称、运行状态和归属筛选，并支持查看服务与容器运行详情。
  - 项目创建和重新部署使用专用进度窗口展示当前阶段，操作结果统一保留在日志中。
  - 配置保存与重新部署分离，可在部署前选择是否拉取最新镜像。

### Changed

- 连续重复的节点注册失败现在按十分钟汇总；运行时操作日志保留 30 天，其他操作日志继续保留 180 天，避免长期重试导致主控数据库持续快速增长。
- 节点状态现在区分生命周期、有效会话和新鲜健康观测，过期租约或陈旧检测结果不再把节点误显示为在线或健康。
- 节点部署、重新部署、检查和卸载在页面请求中断后仍可继续执行；控制服务重启后会根据节点与远端状态恢复未完成任务。
- 删除节点前会保护本地节点、在线节点、活动任务和被其他功能引用的节点；节点接入参数与保留标签校验更加严格。
- 可用性检查结果与当前部署配置版本绑定，配置变更后必须重新检查；部署只有在 Agent 完成注册并建立有效租约后才会成功。
- 首次部署部分失败时会清理本次新增的远端服务和文件并撤销注册凭据；重新部署失败会恢复原有 Agent，卸载也会在确认远端残留已清除后才退役节点。

- 防火墙管理聚焦实际生效的 nftables 与 iptables 规则，不再根据管理工具安装状态推断防护健康度。

- 脚本库只展示用户自定义 Shell 脚本，列表不再加载或展示完整正文。
- 执行节点和超时设置移入执行确认窗口，执行记录与输出改为独立侧边栏。
- 脚本库刷新失败时保留已有数据，快速搜索和切换脚本时仅采用最新响应，并在离开前保护未保存内容。

- 计划任务列表只展示用户自定义任务，并按后端返回的操作能力提供编辑、启停、运行、迁移和删除入口。
- 计划任务列表改为服务端分页与筛选；命令移入按需加载的详情，刷新失败时保留已有数据。
- 计划任务页面改为紧凑工具栏和内部滚动布局，区分首次加载、真实空数据、筛选无结果和后台刷新警告。

- 系统监控趋势按时间范围使用固定服务端分辨率，磁盘与网络速率统一由 Agent 计算。
- 系统监控窗口支持跟随当前节点切换，实时概览与历史趋势分别刷新且不再产生重叠轮询。
- 宿主机终端改为按服务端会话状态展示连接、启动、活动、关闭和退出结果，并支持窗口拖拽时自动调整终端尺寸。
- Docker 客户端支持 API 版本协商和旧版磁盘统计，最低支持 Docker Engine 28.0。
- Docker 概览 CPU 明确区分宿主机容量占比与容器多核占用，内存采用工作集口径。
- Docker 资源采样调整为每分钟一次，网络趋势直接展示采样速率。
- 精简 Docker 系统信息，并优化系统清理过程中的通知与页面交互。
- Docker 网络列表改为轻量加载，并保护系统、Compose 和套件网络不被直接修改。
- Docker 数据卷由对应管理入口维护，套件和 Compose 卷不再允许从卷模块直接删除。
- Docker 镜像分发改为紧凑任务栏和独立节点选择窗口，离线节点不再进入分发目标。
- Docker 镜像列表精简为标签、ID、大小、容器数和创建时间，并由 Agent 保护在用镜像。
- Docker 容器列表改为轻量加载，详情与资源数据按需获取；套件和 Compose 容器统一回到对应管理入口维护。
- Docker 容器终端仅允许在运行中的自定义容器开启，并记录可信用户来源和操作目标。
- Docker 套件和系统项目改为只读展示，生命周期操作统一回到对应管理入口。
- Docker 项目删除会保留命名卷、镜像和宿主机绑定数据，并明确区分配置保存与容器重建。
- 文件管理窗口固定绑定打开时的目标节点；刷新失败保留已有目录数据，快速切换路径时仅采用最新响应。
- 文件编辑器在磁盘内容冲突时保留本地修改并停止覆盖，必须确认重新加载后才能继续保存。
- 进程与网络数据改为按目标节点共享采样，并仅轮询当前活动标签，多个窗口不再重复扫描节点。

### Removed

- 移除主控内置“Web 浏览器”应用；协议仿真规则的 HTML 页面改由套件自身安全预览。

### Fixed

- 修复 VS Code 端口转发、空闲 TCP 或 WebSocket 连接可能使服务卸载等待 90 秒后才被强制终止的问题；卸载会先确认服务进程已经退出再删除文件。
- 修复手动、批量和定时触发可能绕过防重叠限制，以及重复提交同一运行可能再次启动进程的问题。
- 修复请求超时后任务仍在后台运行但无法继续跟踪、Agent 重启后活动运行丢失终态的问题。
- 修复运行结果上报失败后永久丢失、错误节点污染其他节点记录，以及旧运行状态覆盖终态的问题。
- 修复大输出可能占用过多内存或磁盘，以及跨夏令时边界的执行时间计算不准确问题。
- 修复按“等待节点上线”筛选计划任务时接口返回参数错误的问题。

- 修复磁盘分区与整盘重复计数、虚拟网卡混入宿主机网络吞吐的问题。
- 修复首次采样、计数器回退、长时间采样间断和部分采集失败被误显示为零负载的问题。
- 修复节点或时间范围快速切换时旧响应覆盖当前监控数据，以及刷新失败丢失已有趋势的问题。
- 修复宿主机终端旧连接覆盖新会话、异常断开残留 Shell、`exit` 偶发误报连接异常、退出码不准确以及中文输出乱码的问题。
- 套件工作负载创建前检查目标节点宿主机端口，端口冲突时不再创建容器。
- 套件工作负载容器启动失败时自动清理已创建容器，避免遗留停止状态的容器。
- 修复 Compose 项目数量按容器数量重复统计的问题。
- 修复多核 CPU 占用被错误限制为 100% 以及部分采样被静默忽略的问题。
- 修复统计失败被显示为零负载或 Docker 服务停止的问题。
- 修复容器重启、计数器回退和采样间断导致网络趋势失真的问题。
- 修复 Docker 镜像共享层和构建缓存可回收空间统计与 `docker system df` 不一致的问题。
- 修复网络速率低于 `1 B/s` 时单位显示异常的问题。
- 修复节点切换后镜像列表可能保留旧节点数据，以及批量分发部分启动后失去进度跟踪的问题。
- 修复容器列表、详情和资源请求在节点快速切换时被旧响应覆盖的问题。
- 修复容器网络速率由前端累计计数差值产生负值或失真的问题。
- 修复 Docker 项目部分运行被误报为正常运行，以及耗时操作超时后无法继续跟踪的问题。
- 修复 Docker 项目创建、重新部署、伸缩和删除失败时可能遗留不完整状态的问题。
- 修复大目录递归浏览、大文件上传下载可能占用过多内存，以及保存、覆盖或删除中断时可能遗留不完整文件的问题。
- 修复文件上传目标解析与下载响应格式不一致导致传输失败的问题。
- 修复文件已实际保存但仍显示失败、刷新失败遮挡已有内容，以及保存与移动、删除或上传并发冲突的问题。
- 修复进程列表在窗口节点变化时读取错误节点、旧响应覆盖新筛选，以及信号成功投递后仍被误报为失败的问题。

## [0.1.0-alpha.2] - 2026-07-11

### 不兼容变更

- 本版本不兼容 `0.1.0-alpha.1` 的协议仿真内置模块。协议仿真已从主控和 Agent 内置功能拆分为 `seclab.protocol-simulation` Compose 套件，旧的主控仿真接口、内置仿真前端、仿真数据库表和 Agent 内置协议运行器不再作为主控能力提供。
- Alpha 阶段不提供旧版本数据迁移兼容保证。升级到本版本后，协议仿真应通过套件中心安装协议仿真套件，并在套件内重新导入规则包和部署实例。
- 套件、桌面快捷方式和安装任务接口的部分请求格式已调整，前端和调用方需要使用当前版本 API。

### Added

- **协议仿真套件化**
  - 支持由协议仿真套件通过 Agent suite workload API 拉起独立规则容器。
  - 支持套件 workload 容器命名、标签归属、停止删除和卸载清理，避免套件卸载后遗留孤儿容器。
  - 支持由 Agent 在宿主机侧执行 PCAP 取证，并将取证结果返回给套件保存。

- **套件中心**
  - 支持按节点安装、启用、停用和卸载套件，套件状态随当前节点上下文展示。
  - 支持安装进度查询和取消安装。
  - 支持安装前解析 Compose 镜像和 `runtime.images` 依赖镜像，并在目标节点自动准备缺失镜像。
  - 支持套件生命周期事件日志。

- **Docker 管理**
  - 新增镜像仓库配置与拉取流程优化。
  - 增强镜像分发进度和可靠性。
  - 在 Docker 项目和容器列表中展示套件资源标记。
  - 支持解析容器日志中的 ANSI 颜色和文本样式，日志面板与日志弹窗可按终端输出效果展示彩色日志。

- **安全加固**
  - 新增安全入口机制，支持随机登录路径、入口记忆和未携带入口时的根路径提示页。
  - 安装流程支持生成初始账号、密码和安全入口，并在服务首次启动时写入安全配置。
  - 新增验证码、防爆破、密码复杂度和基础密码长度校验。
  - 新增安全设置接口和前端安全设置能力。
  - `slctl` 新增登录地址信息查看、密码修改、用户名修改和安全入口管理能力。

### Changed

- 桌面快捷方式保存接口改为在请求体中传递节点 ID。
- Docker 容器详情、日志、历史指标和终端等操作按容器所属节点转发，避免子节点容器请求落到本地节点。
- 套件 Web 入口启用流程会等待入口就绪后再完成启用。
- 套件代理错误会透传上游失败信息，便于定位套件服务异常。
- 优化应用库内置应用排序，优先展示套件中心、节点管理和核心运维工具。
- 优化默认桌面应用组合，默认展示更贴近 Linux 管理场景的核心工具入口。

### Removed

- 移除主控内置协议仿真 API、前端页面、规则包服务和仿真数据库模型。
- 移除 Agent 内置 HTTP、Redis、SMTP、POP3、IMAP、SSH、FTP、RDP 协议仿真运行器。
- 移除主控内置协议仿真应用图标和相关菜单入口。

### Fixed

- 修复桌面快捷方式交换位置时的布局保存问题。
- 修复删除当前节点后没有自动切回本地节点的问题。
- 修复活跃节点仍显示删除入口的问题。
- 修复套件安装后 Web 入口未就绪导致打开失败的问题。
- 修复应用库中内置应用和套件应用卡片对齐方式不一致的问题。
- 修复发布工作流中的 release tag 格式问题。

## [0.1.0-alpha.1] - 2026-06-28

### Added

- **Web 控制台**
  - 提供桌面式多窗口控制台，支持应用打开、拖动、缩放、置顶、最小化和桌面图标管理。
  - 提供应用库、通知历史、平台日志、系统设置和主题切换能力。
  - 提供中英文界面语言切换。

- **节点管理**
  - 支持本地节点和远程节点纳管。
  - 支持节点注册、健康检查、状态监控、基础资源指标展示和节点退役。
  - 支持基于 mTLS 的主控与 Agent 安全通信。

- **Docker 管理**
  - 支持容器、镜像、数据卷、网络和 Compose 项目的可视化管理。
  - 支持容器日志查看、Compose 项目部署和镜像仓库与代理配置。
  - 对套件专属 Docker 资源展示来源标记。

- **Compose 套件中心**
  - 支持导入 `.slsp` 套件包。
  - 支持套件安装、启用、停用、卸载和删除。
  - 支持套件应用入口注册到应用库和桌面。
  - 支持通过主控代理打开套件 Web 应用，并与套件 SDK 协同完成聚焦、通知和语言同步。

- **协议仿真**
  - 支持协议仿真规则库导入和版本校验。
  - 支持仿真实例创建、部署、运行和状态查看。
  - 支持仿真实例 PCAP 取证和结果查看。

- **任务与脚本**
  - 支持脚本管理、脚本编辑和节点脚本执行。
  - 支持一次性计划任务和定时任务下发到 Agent 执行。

- **主机运维**
  - 提供文件管理、文件编辑、进程管理、磁盘管理、防火墙管理和交互式终端。
  - 支持通过节点执行面完成宿主机运维操作。

- **升级与审计**
  - 支持主控与 Agent 在线升级流程。
  - 支持平台事件日志、运行日志和关键操作审计。

[0.1.0-alpha.2]: https://github.com/seclab-dev/seclab/releases/tag/0.1.0-alpha.1...0.1.0-alpha.2
[0.1.0-alpha.1]: https://github.com/seclab-dev/seclab/releases/tag/0.1.0-alpha.1
